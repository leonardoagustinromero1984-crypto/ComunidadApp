package com.comunidapp.shared.push

/**
 * Stable installation id for iOS PoC push registration.
 * One logical install per app install on device (not per session hash).
 */
fun iosInstallationId(): String = "leover-ios-default-install"
